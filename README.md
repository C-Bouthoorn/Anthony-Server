# Anthony-Server
A server for Anthony


## HOW TO INSTALL

  Run `npm install`

## HOW TO RUN SERVER

  Run `npm start`

## HOW TO BUILD CLIENT

  Run `npm build` (without output)  
  Run `./build.sh` (with output)
